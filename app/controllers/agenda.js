// const Agenda = require("agenda");
// const db = require('../models');
// // const mongoConnectionString = 'mongodb://localhost:27017/FirstMongoAuth';
// const dbConfig = require("./app/config/db.config");


// const agenda = new Agenda({ db: `mongodb://${dbConfig.HOST}:${dbConfig.PORT}/${dbConfig.DB}` });



// agenda.define('check raffle winner', async (job) => {
//     // some code that aggregates the database and send monthly billing report.
//     console.log("siema");
//   });

// (async function () {
// // IIFE to give access to async/await
//     await agenda.start();
//     await agenda.every('5 seconds', console.log("siema"));
// })();


//node-schedule test'
